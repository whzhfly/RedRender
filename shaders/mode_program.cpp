#include "render_program.h"















RenderProgram::RenderProgram(ModelResource& source)
{
    // render_uniform_context.get().
    // source.material.
    // to finish
    render_uniform_context.get()->basecolor_factor = source.material.basecolor_factor;


};
